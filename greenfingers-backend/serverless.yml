service: greenfingers-backend

provider:
  name: aws
  runtime: nodejs12.x
  region: eu-west-1

functions:
  plantList:
    handler: handler.plantApp
    events:
       - http:
          method: get
          path: plants
          cors: true
       - http:
          method: post
          path: plants
          cors: true

